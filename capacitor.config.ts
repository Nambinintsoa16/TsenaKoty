import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tsenaKoty.myapp',
  appName: 'TsenaKoty',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
